'use strict';

describe('myApp.retirement module', function() {

  beforeEach(module('myApp.retirement'));

  describe('retirement controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var retirementCtrl = $controller('retirementCtrl');
      expect(retirementCtrl).toBeDefined();
    }));

  });
});
